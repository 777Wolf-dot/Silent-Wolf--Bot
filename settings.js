// settings.js
const settings = {
  PREFIX: process.env.PREFIX || '.',
  OWNER_JID: process.env.OWNER_JID || '123456789@s.whatsapp.net',  // Replace with your WhatsApp number in jid form
  // Add any other config here:
  // example: MAX_WARNINGS: 3,
};

export default settings;
